// See https://aka.ms/new-console-template for more information
using AbstractFactory;
using System;


class Program
{
    static void Main(string[] args)
    {
        // In the Main method, create an instance of the concrete factory
       // IDisneyFactory cartoonFactory = new Anastasia(); 

        // Create an instance of the LevelManager with the concrete factory
       // CartoonLevel cartoonLevel = new CartoonLevel(cartoonFactory);

        // Use the LevelManager to create and set up the game level
      //  cartoonLevel.CreateLevel();

        // Keep the console window open so that you can see the output
       // Console.ReadLine();
    }
}

